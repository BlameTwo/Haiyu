<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Haiyu.Pages.ToolPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:local="using:Haiyu.Pages"
    xmlns:m="using:Haiyu.ViewModel"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="25">

        <ItemsView IsItemInvokedEnabled="True" ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}">
            <i:Interaction.Behaviors>
                <i:EventTriggerBehavior EventName="ItemInvoked">
                    <i:InvokeCommandAction Command="{x:Bind ViewModel.NavigationToCommand}" />
                </i:EventTriggerBehavior>
            </i:Interaction.Behaviors>
            <ItemsView.ItemTemplate>
                <DataTemplate x:DataType="m:ToolItem">
                    <ItemContainer>
                        <Grid
                            Padding="10"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Background="{ThemeResource AcrylicBackgroundFillColorBaseBrush}"
                            BorderBrush="{ThemeResource SolidBackgroundFillColorBaseAltBrush}"
                            BorderThickness="1"
                            CornerRadius="5">
                            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Grid.RowDefinitions>
                                    <RowDefinition />
                                    <RowDefinition />
                                </Grid.RowDefinitions>
                                <FontIcon
                                    HorizontalAlignment="Center"
                                    FontFamily="Segoe UI Emoji"
                                    FontSize="30"
                                    Glyph="{x:Bind Icon}" />
                                <TextBlock
                                    Grid.Row="1"
                                    HorizontalAlignment="Center"
                                    Text="{x:Bind Name}" />
                            </Grid>
                        </Grid>
                    </ItemContainer>
                </DataTemplate>
            </ItemsView.ItemTemplate>
            <ItemsView.Layout>
                <UniformGridLayout
                    ItemsJustification="Start"
                    ItemsStretch="Uniform"
                    MinColumnSpacing="2"
                    MinItemHeight="120"
                    MinItemWidth="120" />
            </ItemsView.Layout>
        </ItemsView>
    </Grid>
</Page>
